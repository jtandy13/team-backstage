{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22 || 24"
  },
  "scripts": {
    "prestart": "test -f .env || cp .env.example .env",
    "start": "dotenv -e .env -- sh -c 'if command -v pg_isready >/dev/null 2>&1; then pg_isready -h \"$POSTGRES_HOST\" -p \"$POSTGRES_PORT\" || { echo \"PostgreSQL is not ready at $POSTGRES_HOST:$POSTGRES_PORT. Start native PostgreSQL first (e.g. brew services start postgresql@16 or sudo pg_ctlcluster 16 main start), or use yarn start:local.\"; exit 1; }; fi' && dotenv -e .env -- backstage-cli repo start",
    "start:app": "yarn start",
    "start:local": "./scripts/start-local.sh",
    "db:status": "./scripts/db-status.sh",
    "build:backend": "yarn workspace backend build",
    "build:all": "backstage-cli repo build --all",
    "build-image": "yarn workspace backend build-image",
    "tsc": "tsc",
    "tsc:full": "tsc --skipLibCheck false --incremental false",
    "clean": "backstage-cli repo clean",
    "test": "backstage-cli repo test",
    "test:all": "backstage-cli repo test --coverage",
    "test:e2e": "playwright test",
    "fix": "backstage-cli repo fix",
    "lint": "backstage-cli repo lint --since origin/main",
    "lint:all": "backstage-cli repo lint",
    "prettier:check": "prettier --check .",
    "prepare": "husky",
    "new": "backstage-cli new"
  },
  "backstage": {
    "cli": {
      "new": {
        "globals": {
          "license": "UNLICENSED"
        }
      }
    }
  },
  "workspaces": [
    "packages/*",
    "plugins/*"
  ],
  "devDependencies": {
    "@backstage/cli": "^0.36.3",
    "@backstage/cli-defaults": "^0.1.3",
    "@backstage/e2e-test-utils": "^0.1.2",
    "@jest/environment-jsdom-abstract": "~30.2.0",
    "@playwright/test": "^1.32.3",
    "@types/jest": "^30.0.0",
    "dotenv-cli": "^8.0.0",
    "husky": "^9.1.7",
    "jest": "~30.2.0",
    "jsdom": "^27.1.0",
    "lint-staged": "^17.0.8",
    "node-gyp": "^10.0.0",
    "prettier": "^2.3.2",
    "typescript": "~5.8.0"
  },
  "resolutions": {
    "@types/react": "^18",
    "@types/react-dom": "^18"
  },
  "prettier": "@backstage/cli/config/prettier",
  "lint-staged": {
    "*.{js,jsx,ts,tsx,mjs,cjs}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yaml,yml}": [
      "prettier --write"
    ]
  },
  "packageManager": "yarn@4.13.0"
}
